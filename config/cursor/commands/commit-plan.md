# commit-plan

Create an atomic commit split plan from current changes.

Use the `commit-message` skill and force multi-commit planning mode:

- Identify independent concerns in the diff
- Propose ordered commits with one intent each
- Provide one commit message per commit
- Explain why this split improves review/cherry-pickability
